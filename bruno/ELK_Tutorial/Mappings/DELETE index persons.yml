info:
  name: DELETE index persons
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}:9200/persons"
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
