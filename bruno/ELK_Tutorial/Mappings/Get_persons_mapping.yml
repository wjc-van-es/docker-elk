info:
  name: Get_persons_mapping
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}:9200/persons/_mapping"
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
