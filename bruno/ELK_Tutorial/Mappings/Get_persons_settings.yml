info:
  name: Get_persons_settings
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}:9200/persons/_settings"
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
