info:
  name: Get_movies_mapping
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}:9200/movies/_mapping"
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
