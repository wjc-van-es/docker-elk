info:
  name: Persons_FindInDetailsOr
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}:9200/persons/_search"
  body:
    type: json
    data: |-
      {
        "query": {
          "match": {
            "details": {
              "query": "Disney miser",
              "operator": "or"
            }
          }
        },
        "size": 25
      }
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
