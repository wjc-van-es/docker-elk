info:
  name: Insert_bulk_movies
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}:9200/movies/_bulk"
  body:
    type: file
    data:
      - filePath: /home/willem/git/docker-elk/docker-elk-files/movies250.json
        contentType: application/json; charset=utf-8
        selected: true
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
